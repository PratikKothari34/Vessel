# Dependencies
node_modules/
app/node_modules/

# Env / secrets (Turso URL + auth token live here)
.env
.env.local
app/.env

# Local libSQL database + embedded-replica artifacts
*.db
*.db-*
*.sqlite
*.sqlite3
data/
local-replica*
# Cleartext DB backup/temp from the encrypt-at-rest migration (never commit these)
*.plaintext.bak
*.plaintext.bak-*
*.enc.tmp
*.enc.tmp-*

# Build output
out/
dist/
app/out/
app/dist/

# Logs / OS cruft
*.log
.DS_Store
Thumbs.db

# Archives / generated design output (e.g. Stitch UI exports)
*.zip
stitch_*/

# Claude config (not version-controlled)
.claude/
