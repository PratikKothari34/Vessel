{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The main window may invoke the app's own commands, and nothing else.",
  "local": true,
  "windows": ["main"],
  "permissions": ["ipc"]
}
